/**
 * Returns a new Set object from an array.
 *
 * @param {Array} array - The array to be converted to a Set object.
 * @returns {Set} A new Set object containing the elements of the input array.
 */
export default function setFromArray(array) {
  return new Set(array);
}
